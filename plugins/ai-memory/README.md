# ai-memory plugin

Skills, hooks, and a bundled MCP launcher for the standalone memory packages. Run `npm run build` from the repository root. The plugin launcher and its copied migrations are generated under this directory. Configure a loopback `AI_MEMORY_DATABASE_URL` in the host's plugin environment before use.
