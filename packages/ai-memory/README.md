# @aviaratech/ai-memory

Core memory contracts, local PostgreSQL persistence, retrieval, retention, and numbered migrations. The package root is the public API. The `./internal` export is for the companion tools package.

Set an explicit loopback `AI_MEMORY_DATABASE_URL`. The source repository documents API ownership and migration behavior in `docs/api-architecture.md`.
